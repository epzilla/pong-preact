.recruiting {
  display: flex;
  flex-direction: column;
  align-items: center;

  iframe {
    width: 100%;
    height: auto;
    min-height: 500px;

      @media (max-width: 800px) {
      min-height: 300px;
    }
  }
}